{
  "name": "linkbonsai",
  "scripts": {
    "dev": "npm run start:both",
    "start": "cd backend && node server.js",
    "build": "cd frontend/ && npm install && cd ../backend/ && npm install && cd ../ && npm run build"
  }
}